$(".grid").html("<%= escape_javascript(render(@products)) %>");
$("#new-form").removeClass("active");
$(document).ready(function(){
    modalDisplay();
});

    var msg = "投稿が完了しました"
    postSuccessMsg(msg);
    postBtnUp();

