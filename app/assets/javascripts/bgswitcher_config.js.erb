jQuery(function($) {
  $(window).on('load resize', function() {
  if($(window).width() > 559){
    $('#home').bgSwitcher({
    images: ['<%= image_url("home/home-bg1.JPG") %>','<%= image_url("home/home-bg2.JPG") %>','<%= image_url("home/home-bg3.JPG") %>'],
  　 interval: 3000,
  　 loop: true,
  　 effect: "fade", // fade,blind,clip,slide,drop,hide
  　 duration: 1000,
  　 easing: "swing" // linear,swing
   });

  }else if($(window).width() < 559) {
    $('#home-mobile').bgSwitcher({
    images: ['<%= image_url("home/home-bg1.JPG") %>','<%= image_url("home/home-bg2.JPG") %>','<%= image_url("home/home-bg3.JPG") %>'],
  　 interval: 3000,
  　 loop: true,
  　 effect: "fade", // fade,blind,clip,slide,drop,hide
  　 duration: 1000,
  　 easing: "swing" // linear,swing
   });
  };
  });
  });